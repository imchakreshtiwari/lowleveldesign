server:
  port: 8088

spring:

  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        storage_engine: innodb

  datasource:
    url: jdbc:mysql://localhost:3306/tinyURL
    username: shubham
    password: Uber@102030